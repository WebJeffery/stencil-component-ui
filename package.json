{
  "private": true,
  "description": "一个使用stencilJs开发的web component组件库",
  "scripts": {
    "dev:c": "pnpm --filter swc-ui run start",
    "dev:icon": "pnpm --filter @swc-ui/icon run dev",
    "dev:docs": "pnpm --filter docs run dev",
    "dev": "pnpm run dev:icon & pnpm run dev:c",
    "build:c": "pnpm --filter swc-ui run build",
    "build:icon": "pnpm --filter @swc-ui/icon run build",
    "build:docs": "pnpm --filter docs run build",
    "build": "pnpm run build:icon & pnpm run build:c",
    "g": "pnpm --filter swc-ui run generate",
    "g:icon": "pnpm --filter @swc-ui/icon run generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  },
  "dependencies": {
    "taze": "^0.12.0"
  }
}
